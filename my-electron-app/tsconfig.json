{
  "compilerOptions": {
    "module": "NodeNext",                         // ESモジュール対応
    "moduleResolution": "NodeNext",               // Node.jsスタイルの解決方法
    "target": "ES2020",                           // 出力対象のJSバージョン
    "outDir": "./dist",                           // 出力先フォルダ
    "rootDir": "./src",                           // ソースのルート
    "esModuleInterop": true,                      // CJSとの互換性を確保
    "jsx": "react",                               // Reactサポート（.tsx対応）
    "types": ["node", "react", "react-dom"],      // グローバル型定義に含めるもの
    "allowSyntheticDefaultImports": true,         // デフォルトインポート対応
    "resolveJsonModule": true,                    // JSONのインポートを許可
    "strict": true,                               // 厳格モード（型の安全性）
    "noEmit": false,                               // tscコマンドでJSを出力しない
    "allowImportingTsExtensions": false,           // `.tsx` 拡張子付きのimportを許可
    "skipLibCheck": true                          // 依存パッケージの型チェックをスキップ ← ★これが重要！
  },
  "include": [
    "src/**/*",                                   // プロジェクトの全コード
    "src/renderer/electron.d.ts",                 // Electron固有の型定義
    "src/types/react-transition-group-shim.d.ts"  // 手動で追加した型定義
  ]
}
